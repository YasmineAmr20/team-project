<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login</title>
    <style>
            *{
                margin: 0;
                padding: 0;
            }

            header{
                
                height: 100vh;
             -webkit-background-size: cover;
                background-size:cover ;
                background-position:center;
                position: relative;
                
                display: flex;
            }
            header img{
                    width: -webkit-fill-available;
            }
            .container{
                width: 45%;
                height: 343px;
                display: flex;
                flex-wrap: wrap;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                top: 23%;
                position: absolute;
                left: 28%;
            }
            /* inputs */
            .user #email{
                width: -webkit-fill-available;
                border: solid;
                padding: 5px;
                height: 34px;
                font-size: 18px;
                margin-top: 21px;
                margin-bottom: 22px;
                border-color: #070807;
              border-radius: 13px;
            }
            .user #userName:hover{
                border-color: #070807;
            }
            .user #password{
                width: -webkit-fill-available;
                border: solid;
                padding: 5px;
                border-color: #070807;
                height: 34px;
                font-size: 18px;
                margin-bottom: 29px;
                margin-top: 16px;
                border-radius: 13px;

            }
            .user #password:hover{
                border-color: #040505;
            }
          .container .radio{
                    width: 459px;
                    height: 32px;
                   
                    font-weight: bolder;
                    font-size: 21px;
                    text-align: center;
                    margin-left: -51px;
            }
            .radio  #one{
                margin-left: -54px;
                font-family: system-ui;
                font-size: 25px;
                color: white;
                font-weight: 700;

                margin-right: 32px;
            }
            .radio  #two{
                margin-left: -8px;
                font-family: system-ui;
                font-size: 25px;
                color: white;
                font-weight: 700;
                margin-right: 32px;
            }
            .radio  #three{
                margin-left: -8px;
                font-family: system-ui;
                font-size: 25px;
                color: white;
                font-weight: 700;
                margin-right: 32px;
            }
            .radio #one:hover{
                color: gray;
                
            }
            .radio #two:hover{
               color: gray;
                
            }
            .radio #three:hover{
                color: gray;
                
            }
            .radio{
                position: relative;
                left: 83px;
            }
            .log-btn {
                display: flex;
            }

            .log-btn button {
                
       position: relative;
    width: 136px;
    font-weight: bold;
    height: 44px;
    border: solid;
    margin-top: 55px;
    left: 139px;
    color: white;
    background-color: #cff7ff99;
    font-size: large;
    border-radius: 5px;
    font-family: cursive;
          
        }

            .log-btn :hover{
   background-color: rgb(13, 10, 37);
                border-color: rgb(243, 237, 237);
                color: rgb(215, 210, 210);
            }
    </style>
   
       
</head>
    <body>
        <header>
            <img src="../../images/img.jpg" alt="image">

            <div class="container">    
                    <form action="" method="post" name="form1">
                        <div class="user">
                            <input type="email" name="email" id="email" placeholder="xxx@gmail.com">
                        
                            <input type="password" name="password" id="password" placeholder="password">
                        </div>
                
                    
                        <div class="radio">
                            <label id="one">
                                <input type="radio"   id="admin" name="user" value="admin">admin
                            </label>
                            
                            <label id="two">
                                <input type="radio" id="doctor" name="user" value="doctor">doctor
                            </label>
                            
                            <label id="three">
                                <input type="radio" id="student" name="user" value="student"> student
                            </label>
                        
                        </div> 
                    
                       <div class="log-btn">
                        {{!-- <input type="submit" value="Login">
                        <input type="submit" value="Register"> --}}
                        <button id="log_btn"   >Login</button>
                        {{!-- <button type="submit" onclick="window.location.href='/register'" >Register</button> --}}

                    </div>
                        {{!-- <button type="button" onclick="check()" >Register</button> --}}

                </form>
             
            </div>


        
        </header>
        {{!-- <script>
        const loginButton = document.getElementById('log_btn');

        loginButton.addEventListener('click', () => {
            const email = document.getElementById('email').value;

            const password =window.getElementById('password').value;

            // Check if the username and password are correct
            const user = [
            {
                email: 'email',
                password: 'password'
            },
            ];

            for (const u of user) {
            if (u.email === email && u.password === password) {
                // Login successful
                window.location.href = '/homepage';
                return;
            }
            }

            // Login failed
            notifier.notify({
            title: 'Login Failed',
            message: 'Invalid username or password',
            });
        });
        </script> --}}
        {{!-- <script>
               const rad_admin=document.getElementById("admin");
                const rad_doctor=document.getElementById("doctor");
                const rad_student=document.getElementById("student");
                const check_email=document.getElementById("email");
                const check_password=document.getElementById("password");
                const btn=document.getElementById("log_btn");
            
                function fun(){
                        const value_email = check_email.value;
                        const value_pass = check_password.value;

                    if(value_email=="admin@gmail.com" && value_password=="admin")
                    {
                        if(rad_admin.checked){
                        const value = rad_admin.value;
                        window.location.href="/departments";
                        console.log(value);
                        }else{
                            alert("you not admin");
                        }
                    }else{
                        alert("wroooong")
                    }
                     
                     }
               
               
                    if(rad_admin.checked==true){
                        const value = rad_admin.value;
                        window.location.href="/homepage";
                        console.log(value);
                     }
                

        </script> --}}
         <script>
        const btn = document.querySelector('#log_btn');        
        const radioButtons = document.querySelectorAll('input[name="user"]');
        
        function log(){
             for (const radioButton of radioButtons) {
                if (radioButton.checked) {
                    if(radioButton.value=="admin"){
                        alert("welcome admin")
                         break;
                    }else if (radioButton.value=="student"){
                            alert("welcome "+{{email}})            
                                break;
                    }else if (radioButton.value=="doctor"){
                            alert("welcome "+{{email}})            
                         break;
                    }
                    
                   
                }else{
                    console.log("error not selected option ");
                }
            }
        }

           
            // show the output:
           // output.innerText = selectedSize ? `You selected ${selectedSize}` : `You haven't selected any size`;
       
    </script>
    </body>
</html>
